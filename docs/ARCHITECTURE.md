# Architecture (Canonical Pointer)

Primary architecture source:
- `../ARCHITECTURE.md`

This docs directory uses that file as architecture truth.
For contract behavior, see `CONTRACT.md`.
For extension seams, see sections in `../ARCHITECTURE.md` and code ports under `src/ports/*`.
